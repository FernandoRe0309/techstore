<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TechStore</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav>
        <div class="logo">TechStore</div>
        <div>
            <a href="/">Catálogo</a>
            <% if (user) { %>
                <a href="/history">Historial</a>
                <a href="/logout">Salir (<%= user.username %>)</a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/register">Registro</a>
            <% } %>
            <a href="/cart">🛒 Carrito <span id="nav-cart-count">(<%= typeof cartQty !== 'undefined' ? cartQty : 0 %>)</span></a>
        </div>
    </nav>
    <div class="container">